
package com.ivpulse.kpi.service.util;

/**
 * Placeholder for global average rate retrieval.
 * In a real project, wire this to a repository query (e.g., SELECT AVG(rate_amount) FROM rate_card).
 */
public class RateUtil {
    public static double globalAvgRate() {
        return 1.0; // Replace with repository-backed value if needed.
    }
}
